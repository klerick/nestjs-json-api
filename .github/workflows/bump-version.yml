name: ⚙️ Bump version

on:
  workflow_call:
    inputs:
      projects:
        description: "Project list"
        required: true
        type: string
      specifier:
        description: "Version bump: patch/minor/major or an exact version. Empty derives it from the commits."
        required: false
        type: string
        default: ""
      beta-release:
        description: 'Is beta release?'
        required: false
        type: boolean
        default: false
      dry-run:
        description: 'Is dry run?'
        required: false
        type: boolean
        default: false
    secrets:
      NPM_TOKEN:
        required: true
      GITHUB_TOKEN_SECRET:
        required: true
      PAT:
        required: true

env:
  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  GITHUB_TOKEN: ${{ secrets.PAT }}
  NPM_CONFIG_PROVENANCE: true

jobs:
  bump-version:
    runs-on: ubuntu-latest
    permissions:
      contents: "write"
      actions: "write"
      id-token: "write"
    steps:
      ### Use only classic token
      - name: Check PAT
        run: |
          if [[ -z "${{ secrets.PAT }}" ]]; then
            echo "### ❌ PAT Error" >> $GITHUB_STEP_SUMMARY
            echo "The \`PAT\` secret is not set, which is required for git operations." >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "### ✅ PAT Found" >> $GITHUB_STEP_SUMMARY
          fi
        shell: bash
      - name: Settings git
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT }}
      - name: Npm install
        uses: ./.github/actions
      - name: Bump version
        env:
          # Through the environment rather than straight into the script, so the
          # value cannot run as shell.
          SPECIFIER: ${{ inputs.specifier }}
        run: |
          # Deliberately unquoted: an empty specifier has to disappear, not
          # arrive as an empty argument. Supplying one is the only way a package
          # leaves a prerelease -- conventional commits keep bumping the
          # prerelease counter forever on their own.
          if [ "${{ inputs.beta-release }}" = "true" ]; then
            npx nx release $SPECIFIER --skip-publish --projects=${{ inputs.projects }} --dry-run=${{ inputs.dry-run }} --preid=beta
          else
            npx nx release $SPECIFIER --skip-publish --projects=${{ inputs.projects }} --dry-run=${{ inputs.dry-run }}
          fi
        shell: bash

